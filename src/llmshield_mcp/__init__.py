"""toolgate: capability gating for MCP agents, and the measurements that argue for it.

The import package keeps its original name (`llmshield_mcp`) while the
distribution and CLI are `toolgate`: renaming the package would touch every
import for no user-visible gain, and the distribution name is what anyone
actually types.
"""

__version__ = "0.1.0"
