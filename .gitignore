# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets
.env

# Reused LLMShield model artifacts. Never committed: the trained weights are
# not publishable (see docs/PINNING.md and README "Reproducibility" section).
models/
*.joblib
*.safetensors
*.onnx

# Runtime state
*.sqlite
*.sqlite3
logs/

# Fetched benchmark corpora (MIT; re-fetch with scripts/benchmark_rules.py)
corpus/external/

# V0/V3 training-data reference corpus, for decontamination (M6). Mixed
# licenses, not ours to redistribute -- see config/decontamination.yaml.
corpus/reference/

# GAUGE harness output (M7): scores.csv + calibration_report.json per
# gauge-run. Regenerable; publishing a specific run's numbers is a
# deliberate operator decision, not something a milestone commit makes
# unasked (same reasoning as the corpus JSONL export, M6).
results/
